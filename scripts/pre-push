#!/usr/bin/env bash
# Git pre-push hook (installed by `make hooks`): run the same checks CI runs.
# Skip once with: git push --no-verify
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
echo "pre-push: running make check (same as CI)…"
make check
